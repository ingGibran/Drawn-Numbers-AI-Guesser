[project]
name = "backend"
version = "0.1.0"
description = "Number Guesser App Backend"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]",
    "uvicorn",
    "python-multipart",
    "Pillow",
    "numpy",
    "onnxruntime"
]
